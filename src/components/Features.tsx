import React from 'react';

const Features = () => {
    return (
        <section className="w-full h-80 flex items-center justify-center border border-gray-800 rounded-lg my-4">
            <h2 className="text-2xl font-bold text-gray-400">Features Section</h2>
        </section>
    );
};

export default Features;
